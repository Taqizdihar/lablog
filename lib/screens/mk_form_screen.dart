import 'package:flutter/material.dart';

class MkFormScreen extends StatelessWidget {
  const MkFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Form Mata Kuliah')),
      body: const Center(child: Text('Form Mata Kuliah (Boilerplate)')),
    );
  }
}
