import 'package:flutter/material.dart';

class PengamatanScreen extends StatelessWidget {
  const PengamatanScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Pengamatan Screen')),
      body: const Center(child: Text('Pengamatan Screen (Boilerplate)')),
    );
  }
}
