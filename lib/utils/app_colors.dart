import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const slate900 = Color(0xFF1E293B); // AppBar, teks utama, FAB
  static const slate700 = Color(0xFF334155); // Heading, icon aktif
  static const sage    = Color(0xFF16A34A); // Aksen utama, tombol

  // Background
  static const bgPage  = Color(0xFFF1F5F9); // Scaffold background
  static const bgCard  = Color(0xFFFFFFFF); // Card, form
  static const border  = Color(0xFFE2E8F0); // Border, divider

  // Teks
  static const textPrimary     = Color(0xFF1E293B);
  static const textSecondary   = Color(0xFF475569);
  static const textMuted       = Color(0xFF64748B);
  static const textPlaceholder = Color(0xFF94A3B8);

  // Sage scale
  static const sageBg     = Color(0xFFDCFCE7); // Badge background
  static const sageBorder = Color(0xFFBBF7D0); // Badge border
  static const sageText   = Color(0xFF14532D); // Teks di badge sage

  // Status badge
  static const selesaiBg   = Color(0xFFDCFCE7);
  static const selesaiText = Color(0xFF14532D);
  static const draftBg     = Color(0xFFFEF9C3);
  static const draftText   = Color(0xFF713F12);
  static const baruBg      = Color(0xFFDBEAFE);
  static const baruText    = Color(0xFF1E40AF);
  static const revisiBg    = Color(0xFFFFE4E6);
  static const revisiText  = Color(0xFF881337);

  // Label warna mata kuliah (5 pilihan)
  static const mkBlue   = Color(0xFFDBEAFE);
  static const mkGreen  = Color(0xFFDCFCE7);
  static const mkYellow = Color(0xFFFEF9C3);
  static const mkPurple = Color(0xFFF3E8FF);
  static const mkRose   = Color(0xFFFFE4E6);
}
